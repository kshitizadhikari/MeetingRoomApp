@using RoomApp.Utility.ViewModels;
@using RoomApp.Models.Enum;
@model RoomBookingParticipantVM
@{
    ViewData["Title"] = "Edit Room Booking";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div>
    <h1>Add Participant Page</h1>
</div>


<div>
    <div asp-validation-summary="All"></div>
    <form asp-area="BasicUser" asp-controller="Home" asp-action="AddParticipant" method="post">
        <input type="hidden" asp-for="@Model.Room.Id" value="@Model.Room.Id" />
        <input type="hidden" asp-for="@Model.Room.Name" value="@Model.Room.Name" />
        <input type="hidden" asp-for="@Model.Room.Size" value="@Model.Room.Size" />
        <input type="hidden" asp-for="@Model.Room.Status" value="@Model.Room.Status" />
        <input type="hidden" asp-for="@Model.BookingId" value="@Model.BookingId" />
        <input type="hidden" asp-for="@Model.StartTime" value="@Model.StartTime" />
        <input type="hidden" asp-for="@Model.EndTime" value="@Model.EndTime" />
        <div class="mb-3">
            <label asp-for="@Model.UserId"></label>
            <select asp-for="@Model.UserId" asp-items="@(new SelectList(ViewBag.Participant, "Id", "FirstName"))" required>
                <option value="">Select a User</option>
            </select>
            <span asp-validation-for="@Model.UserId" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <button type="submit">Add Participant</button>
        </div>
    </form>
</div>

@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}